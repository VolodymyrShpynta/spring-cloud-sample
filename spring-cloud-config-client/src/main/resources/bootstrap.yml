spring.application.name: config-client

spring.profiles.active: development

spring:
  cloud:
    config:
      uri: http://localhost:8888
      username: root
      password: s3cr3t
      fail-fast: true
